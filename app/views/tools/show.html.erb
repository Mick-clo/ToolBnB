<div class="tool-container">
  <%= cl_image_tag @tool.photo.key, height: 350, width: 450, crop: :fill %>
  <h1><%= @tool.title %></h1>
  <p><strong>Price: </strong><%= number_to_currency(@tool.price, unit: '€', format: "%n%u", precision: 2) %></p>
  <p><strong>Description:</strong> <%= @tool.description %></p>

  <div class="buttons-container">
    <%= link_to 'Edit', edit_tool_path(@tool), class: "edit" %>
    <%= link_to 'Book this Tool', new_tool_booking_path(@tool), class: "book" %>
    <%= link_to 'Delete', tool_path(@tool), method: :delete, data: { confirm: 'Are you sure?' }, class: "delete" %>
    <%= link_to 'Back', tools_path, class: "back" %>
  </div>
</div>
